

1. sudo apt update

2. sudo apt upgrade

3. sudo apt install mysql-server

4. sudo service mysql start

5. sudo mysql_secure_installation

6. sudo mysql -u root -p

Basic MySQL Usage:

CREATE DATABASE my_sql_practice;

USE my_sql_practice;

CREATE TABLE Employees (ID INT PRIMARY KEY,Name VARCHAR(100),Department VARCHAR(50),Salary DECIMAL(10, 2));

INSERT INTO Employees (ID, Name, Department, Salary) VALUES (1, 'Gopi Chappidi', 'BCP', 75000.00);

SELECT * FROM Employees;

ELECT Name, Salary FROM Employees WHERE Department = 'BCP';

UPDATE Employees SET Salary = 80000.00 WHERE ID = 1;

INSERT INTO Employees (ID, Name, Department, Salary) VALUES (1, 'Gopi', 'MCP', 75000.00);


DELETE FROM Employees WHERE ID = 2;

SHOW TABLES;

DROP TABLE Employees;

SHOW DATABASES;

DROP DATABASE database_name;

exit;

sudo service mysql status

sudo service mysql start

sudo mysqldump -u root -p my_sql_practic > my_sql_practic.sql

mysqldump -h 127.0.0.1 -u root -p my_sql_practic > my_sql_practic.sql

mysqldump -h localhost -P 3306 -u root -p my_sql_practic > my_sql_practic.sql

run sample .sql file from terminal:

sudo mysql -u root -p < basic_setup.sql
